module examples.c14.bitstruct_native;

bitstruct Status: u8 {
    mode: 3;
    code: 4;
    spare: 1;
}

fn configure() -> Status {
    var status: Status = Status(0u8);
    status.mode = 5u8;
    status.code = 9u8;
    return status;
}

fn main() -> i32 {
    val status = configure();
    if (status.mode != 5u8) { return 1i32; }
    if (status.code != 9u8) { return 2i32; }
    return 0i32;
}
