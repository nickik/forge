module examples.conformance.negative.unreachable_match_arm;

enum Color { Red, Green }

fn color_code(color: Color) -> i32 {
    return match (color) {
        Color::Red => 1,
        Color::Red => 2,
        Color::Green => 3,
    };
}
