module examples.conformance.literal_trailing_commas;

struct Pair { x: i32; y: i32; }

fn main() -> i32 {
    val values = [1, 2, 3,];
    val point = Pair{x: 1, y: 2,};
    val nested = [[1, 2,], [3, 4,],];
    return values[0];
}
