module examples.conformance.match_block_arms;

fn classify(x: i32) -> i32 {
    return match (x) {
        0 => 0,
        1..=9 when x != 5 => {
            val y: i32 = x + 1;
            return y;
        },
        5 | 10 => 50,
        _ => 100,
    };
}
