module examples.conformance.field_defaults_enum_values;

struct Config {
    width: u32 = 80u32;
    height: u32 = 25u32;
    enabled: bool = true;
}

enum Flags {
    None = 0,
    Read = 1,
    Write = 2,
    All = 3,
}

fn main() -> i32 { return 0; }
