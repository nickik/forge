module examples.conformance.metadata_forms;

@inline()
@overflow(checked)
@{
    :owner "compiler"
    :since #version "1.0"
}
fn checked(value: u32) -> u32 {
    return value + 1u32;
}

@range(0..=100)
type Percent = u8;
