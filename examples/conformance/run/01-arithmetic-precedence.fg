module examples.conformance.arithmetic_precedence;

fn main() -> i32 {
    val a: i32 = 2 + 3 * 4;
    val b: i32 = (2 + 3) * 4;
    val c: i32 = 40 / 5 + 7 % 4;

    if (a == 14 && b == 20 && c == 11) {
        return 0;
    } else {
        return 1;
    }
}
